/**
 * Central export file for all utility functions.
 * 
 * This file re-exports all utilities from individual utility files
 * for convenient importing throughout the application.
 */

export { removeAccents, normalizeSearchText } from './normalize';
export { formatDuration, formatDurationDays } from './formatters';
