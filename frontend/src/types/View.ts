/**
 * View mode types for the catalog display.
 * 
 * These types define the available view modes for displaying books.
 */

/**
 * View mode type - either grid (cards) or table view.
 */
export type ViewMode = 'grid' | 'table';
