/**
 * Theme mode types for the application.
 * 
 * These types define the available theme modes.
 */

/**
 * Theme mode type - either light or dark mode.
 */
export type ThemeMode = 'light' | 'dark';
